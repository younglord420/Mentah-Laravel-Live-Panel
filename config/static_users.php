<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Static user auth (open / trial mode)
    |--------------------------------------------------------------------------
    |
    | User login does not check a fixed account list. Any valid email +
    | non-empty password can open a session (for testing).
    |
    */

];
